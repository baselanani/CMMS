import { useState, useEffect, useRef } from "react";

export default function WorkOrders({location}) {
	function WorkOrders(){
	}
  return (
				<div className="flex-col">
						Work Orders - {location}
				</div>
  );
}
